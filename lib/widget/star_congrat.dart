import 'package:flutter/material.dart';

class StarCongratWidget extends StatelessWidget {
  const StarCongratWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
